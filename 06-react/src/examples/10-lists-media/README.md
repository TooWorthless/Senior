# 10 · Lists & Media

## Ключевые концепции

- Виртуализация — рендерить только видимые элементы
- Реализация виртуального списка с нуля (scrollTop + slicing)
- `react-window` / `@tanstack/react-virtual` — production подходы
- Lazy loading изображений: `loading="lazy"`, IntersectionObserver, blur-up
- `<video>` через `useRef` — императивный API

## Вопросы на интервью

1. Зачем виртуализация и как она работает?
2. Как реализовать infinite scroll?
3. Что такое progressive image loading (blur-up)?
4. Как управлять видео/аудио через ref?
